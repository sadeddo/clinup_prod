{% extends 'base.html.twig' %}

{% block title %}ImgTask{% endblock %}

{% block body %}
    <h1>ImgTask</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ img_task.id }}</td>
            </tr>
            <tr>
                <th>FilePath</th>
                <td>{{ img_task.filePath }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_img_task_index') }}">back to list</a>

    <a href="{{ path('app_img_task_edit', {'id': img_task.id}) }}">edit</a>

    {{ include('img_task/_delete_form.html.twig') }}
{% endblock %}
