<form method="post" action="{{ path('app_img_task_delete', {'id': img_task.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ img_task.id) }}">
    <button class="btn">Delete</button>
</form>
