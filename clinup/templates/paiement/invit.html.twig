{% extends 'base1.html.twig' %}

{% block title %}Logement index{% endblock %}

{% block body %}
<style>
   .container {
    margin-top:3%;
 width: 32%;/* or the width you prefer */
  border: 1px solid #ccc;
  padding: 10px;
  border-radius: 4px;
}

#card-element {
  border: 1px solid #ccc;
  padding: 10px;
  border-radius: 4px;
  background: white;
}

button#submit {
  background-color: #10248c; /* Stripe's brand color */
  color: white;
  border: none;
  border-radius: 4px;
  padding: 12px 16px;
  margin-top: 12px;
  font-size: 16px;
  font-weight: 600;
  cursor: pointer;
  display: block;
  transition: background-color 0.2s;
      width: 100%;
}

button#submit:hover {
  background-color: #10248c; /* a darker shade of Stripe's brand color */
}
        .users{
            width:70%;
        }
        .space{
            width:10%;
        }
        .details{
            width:30%;
            border:1px solid rgba(0, 0, 0, 0.16);
            border-radius: 6px;
            padding-left: 1%;
            padding-right: 1%;
            padding-top: 1%;
            padding-bottom: 1%;
        }
        .conversations{
            width:30%;
            min-height: 100vh;
            border:1px solid rgba(0, 0, 0, 0.16);
        }
        .detail{
            margin-left:7%;
        }
        .search{
            width: 60%;
        }
        .message{
            width: 94%;
        }
        .Search{
            width: 89%;
            margin-left: 18%;
        }
        .cards{
            margin-top:4%;
        }
        .card{
            width: 20rem;
            margin-left:6%;
            margin-bottom:7%;
            display:contents;
            }
        .collapse{
            margin-left:75%;
        }
        .card-title {
            display: flex;
            justify-content: space-between; /* Pour espacer les deux éléments autant que possible */
        }

        .card-title h4.text-end {
            margin-left: auto; /* Déplace la deuxième h4 à la fin de la ligne */
        }
        .search-box {
  background: rgba(38, 32, 32, 0.11);
  padding: 10px 13px;
}
.search-box .input-wrapper {
  background: #fff;
  border-radius: 40px;
}
.icon-button {
    background: none; /* Supprime l'arrière-plan du bouton */
    border: none; /* Supprime la bordure du bouton */
    padding: 0; /* Supprime le rembourrage du bouton */
    cursor: pointer; /* Change le curseur pour indiquer qu'il est cliquable */
}
.friend-drawer {
  padding: 10px 15px;
  display: flex;
  vertical-align: baseline;
  background: #fff;
  transition: 0.3s ease;
}
.friend-drawer--grey {
  background: #eee;
}
.friend-drawer .text {
  margin-left: 12px;
  width: 70%;
}
.friend-drawer .text h6 {
  margin-top: 6px;
  margin-bottom: 0;
}
.friend-drawer .text p {
  margin: 0;
}
.friend-drawer .time {
  color: grey;
}
.friend-drawer--onhover:hover {
  background: #74b9ff;
  cursor: pointer;
}
.friend-drawer--onhover:hover p,
.friend-drawer--onhover:hover h6,
.friend-drawer--onhover:hover .time {
  color: #fff !important;
}

hr {
  margin: 5px auto;
  width: 60%;
}

.chat-bubble {
  padding: 10px 14px;
  background: #eee;
  margin: 10px 30px;
  border-radius: 9px;
  position: relative;
  animation: fadeIn 1s ease-in;
}
.chat-bubble:after {
  content: "";
  position: absolute;
  top: 50%;
  width: 0;
  height: 0;
  border: 20px solid transparent;
  border-bottom: 0;
  margin-top: -10px;
}
.chat-bubble--left:after {
  left: 0;
  border-right-color: #eee;
  border-left: 0;
  margin-left: -20px;
}
.chat-bubble--right:after {
  right: 0;
  border-left-color: #74b9ff;
  border-right: 0;
  margin-right: -20px;
}
@keyframes fadeIn {
  0% {
    opacity: 0;
  }
  100% {
    opacity: 1;
  }
}

.profile-image {
  width: 50px;
  height: 50px;
  border-radius: 40px;
}

.offset-md-9 .chat-bubble {
  background: #74b9ff;
  color: #fff;
}

.chat-box-tray {
  background: #eee;
  display: flex;
  align-items: baseline;
  padding: 10px 15px;
  align-items: center;
  margin-top: 19px;
  bottom: 0;
}
.chat-box-tray input {
  margin: 0 10px;
  padding: 6px 2px;
}
.chat-box-tray i {
  color: grey;
  font-size: 30px;
  vertical-align: middle;
}
.chat-box-tray i:last-of-type {
  margin-left: 25px;
}
.conv {
    text-align: center; /* Centre le texte horizontalement */
    margin-left: 31%;
    margin-top:-12%;
}
.center{
    font-size: 21vh;
color: rgb(3, 23, 102);
}
a {
    text-decoration: none; /* Supprime le soulignement des liens */
    /* Ajoutez d'autres styles personnalisés pour les liens si nécessaire */
}
.nom{
  display: flex;
  flex-direction: column;
  justify-content: center; /* Centre verticalement */
}
.noborder {
    border: none; /* Supprime la bordure */
    background: none; /* Supprime le fond */
    padding: 0; /* Supprime le rembourrage */
    cursor: pointer; /* Change le curseur pour montrer que le bouton est cliquable */
}
.bi{
    margin-left: 0% !important;
}
.inputs
{
 margin: auto; /* Important */
        text-align: center;
        padding: 15% 0;
        width:35%;
}
.b2{
    background-color: #FF385C;
    border-color: #FF385C;
    font-size: 19px;
}
button#submit{
   background-color: #FF385C;
    border-color: #FF385C;
    font-size: 19px;
}
button#submit:hover {
background-color: #FF385C;
}
.bi{
  color: #FF385C;
}
h4{
  font-size: 1.3rem;
}
.badge{
  color:black;
}
    </style>
<main class="main-container">
    <div class="container" style="width:95%;background-color:white;margin:auto;border-radius: 4px;margin-top: 2%;height: auto;">
     <h4>Réservation:</h4>
        <div class="detail">
                    <p><i class="bi bi-calendar-event"></i> {{reservation.date|date('d M y')}} à {{reservation.heure|date('H:i')}}</p>
                    <p><i class="bi bi-clock"></i> {{reservation.nbrHeure}} de service</p>
                </div>
         <h4>Prestataire:</h4>
        <div class="detail">
                    <p><i class="bi bi-person-fill"></i> {{prestataire.firstname}} {{prestataire.lastname}}</p>
                    <p><i class="bi bi-currency-exchange"></i> {{prestataire.prix}}€</p>
                </div>

    <form id="payment-form">
      
  <button class="b2">Payer</button>
    </form>
    </div>
</main>

<script src="https://js.stripe.com/v3/"></script>
<script>
    var stripe = Stripe('pk_test_51PC23OGrwysY3nEfKzPhtsCMxudBmcgUNrjzxPNCJYk5LPdxrjLTcz4LD9DAiVHJe62DmFFerQnKBzTQmskNG1kX0053s31F6s');
    var elements = stripe.elements();

    var form = document.getElementById('payment-form');
    form.addEventListener('submit', function(event) {
        event.preventDefault();
        var reservationId = {{ reservation.id|json_encode }};

        fetch(`/create-checkout-session/${reservationId}`, {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
            },
            body: JSON.stringify() // Montant en centimes
        }).then(function(response) {
            return response.json();
        }).then(function(data) {
            if (data.error) {
                alert(data.error);
            } else {
                stripe.redirectToCheckout({
                    sessionId: data.id
                }).then(function(result) {
                    if (result.error) {
                        alert(result.error.message);
                    }
                });
            }
        });
    });
</script>

{% endblock %}